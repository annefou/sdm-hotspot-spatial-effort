# Single source of truth for the runtime — used by local dev, Docker, and CI.
# Add every dependency a notebook imports here. Local "kitchen-sink" envs
# (e.g. pangeo) hide missing deps; CI builds strictly from this file and will
# silently produce empty notebook cells if anything is missing.
#
# pixi.lock (committed alongside this file) pins every package to an exact
# build hash per platform. Regenerate it with `pixi install` after editing
# this file; commit both files in the same commit.

[workspace]
name = "sdm-hotspot-spatial-effort"
# bioconda hosts snakemake (and downstream rule-engine ecosystem); conda-forge
# is the priority channel for everything else.
channels = ["conda-forge", "bioconda"]
platforms = ["linux-64", "osx-arm64", "win-64"]

[dependencies]
python = "3.12.*"
# --- core scientific stack ---
# elapid 1.0.3 needs numpy>=2.0 and scikit-learn>=1.2,<1.6 — pin to stay solvable.
numpy = ">=2.0"
pandas = ">=2.0"
xarray = ">=2024.6"
scipy = "*"
matplotlib = "*"
pyarrow = "*"
scikit-learn = ">=1.2,<1.6"   # MaxEnt feature transforms / logistic backend (elapid)
# --- notebook execution + Jupyter Book ---
jupytext = "*"
jupyterlab = "*"
nbclient = "*"
ipykernel = "*"
# --- pipeline driver ---
snakemake = "*"
# --- intermediate storage formats (NEVER .npz — DOMAIN.md) ---
netcdf4 = "*"
zarr = "*"
dask = "*"
# --- reading R .rds files in pure Python (no R) — disdat reproduction arm ---
pyreadr = "*"
# --- geographic I/O: EU Article 12 polygons, EOO hulls, CHELSA rasters ---
geopandas = ">=1.0"
shapely = "*"
pyproj = ">=3.0"
rasterio = ">=1.2.1"          # windowed /vsicurl reads of CHELSA v2 GeoTIFFs
fiona = "*"
pyogrio = "*"                 # fast GPKG reader (Article 12 layer)
rtree = ">=0.9"               # elapid spatial sampling
tqdm = "*"
# --- map plotting ---
cartopy = "*"
# --- HTTP for direct downloads ---
requests = "*"

# Pip-only deps. Conda-forge is preferred where available.
[pypi-dependencies]
pygbif = "*"           # GBIF occurrence downloads
# HEALPix on geographic data (NESTED, WGS84-aware) — DOMAIN.md mandate.
healpix-geo = ">=0.1.2"
# Phillips-style presence-background MaxEnt (pure-Python, CPU) — the SDM engine.
elapid = ">=1.0.3"

[feature.docs.dependencies]
mystmd = "*"

[feature.tests.dependencies]
pytest = "*"

[environments]
default = { solve-group = "default" }
docs = { features = ["docs"], solve-group = "default" }
tests = { features = ["tests"], solve-group = "default" }

[tasks]
run = "snakemake --cores 1"
book = "myst build --html"
lab = "jupyter lab"
